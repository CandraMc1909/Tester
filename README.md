#install bot
```bash
pkg update && pkg upgrade -y
pkg install nodejs python git -y
git clone https://github.com/CandraMc1909/Tester.git
cd tester
npm init -y
npm install @whiskeysockets/baileys pino qrcode-terminal
```
